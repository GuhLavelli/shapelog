<% variant ||= "primary" %>
<% size ||= "md" %>
<%= tag.button type: (local_assigns[:type] || "button"), class: button_classes(variant:, size:, extra: local_assigns[:class]), **local_assigns.except(:variant, :size, :type, :class) do %>
  <%= content %>
<% end %>
