<div class="rounded-2xl shadow-sm bg-white dark:bg-slate-900 p-5">
  <p class="text-xs font-medium text-slate-500 dark:text-slate-400 uppercase tracking-wide"><%= label %></p>
  <p class="mt-1 text-3xl font-bold text-slate-900 dark:text-slate-100"><%= value.presence || "—" %></p>
  <% if local_assigns[:delta].present? %>
    <% delta_positive = delta.to_s.start_with?("+") || (delta.respond_to?(:positive?) && delta.positive?) %>
    <p class="mt-1 text-sm font-medium <%= delta_positive ? 'text-[#10b981]' : 'text-rose-500' %>">
      <%= delta %>
      <% if local_assigns[:hint].present? %>
        <span class="font-normal text-slate-400"><%= hint %></span>
      <% end %>
    </p>
  <% end %>
</div>
