<%= render "shared/page_header",
    title: "Editar alerta",
    subtitle: "Ajuste as configurações do alerta de #{@alert.email? ? 'e-mail' : 'WhatsApp'}" %>

<%= render "shared/card" do %>
  <%= render "alerts/form", alert: @alert %>
<% end %>
